
// This file is intentionally left blank.
// The quote creation functionality has been moved into the QuoteBuilder component,
// which is rendered as a dialog from the main quotes page.
// This prevents a route conflict and simplifies the application structure.
export default function RemovedNewQuotePage() {
  return null;
}
